# Placeholder credentials. Override with real values at deploy time, e.g.:
#   kubectl create secret generic clickhouse-secret -n optikk \
#     --from-literal=password='<strong-pass>' --dry-run=client -o yaml | kubectl apply -f -
apiVersion: v1
kind: Secret
metadata:
  name: clickhouse-secret
type: Opaque
stringData:
  password: clickhouse123
